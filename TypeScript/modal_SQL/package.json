{
	"name": "modal-sql",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev": "vite",
		"build": "tsc && vite build",
		"preview": "vite preview",
		"clear": "rm -rf dist",
		"build:receipt": "vite build --config vite.receipt.config",
		"build:shipping_container_insight": "vite build --config vite.shipping_container_insight.config",
		"build:shipment_line_insight": "vite build --config vite.shipment_line_insight.config",
		"build:shipment_insight": "vite build --config vite.shipment_insight.config",
		"build:work_insight": "vite build --config vite.work_insight.config.ts",
		"build:inventory_insight": "vite build --config vite.inventory_insight.config.ts",
		"build:inventory_insight-del": "rm -rf dist/inventory_insight && vite build --config vite.inventory_insight.config.ts",
		"build:popup": "vite build --config vite.popup.config.ts",
		"build:all": "npm run build:receipt && npm run build:shipping_container_insight && npm run build:shipment_line_insight && npm run build:shipment_insight && npm run build:inventory_insight && npm run build:popup",
		"build:all-del": "npm run clear && npm run build:receipt && npm run build:shipping_container_insight && npm run build:shipment_line_insight && npm run build:shipment_insight && npm run build:inventory_insight && npm run build:popup"
	},
	"devDependencies": {
		"@types/node": "^24.3.1",
		"typescript": "~5.8.3",
		"vite": "^7.1.2"
	}
}
